# my-app (Gemini 動作テスト)

## セットアップ
1) 依存関係インストール
npm install

2) 環境変数を設定
cp .env.example .env.local
# .env.local の GEMINI_API_KEY を自分のキーに差し替える

3) 起動
npm run dev

## 動作確認
http://localhost:3000 にアクセスして質問→回答が返ることを確認
